#!/bin/sh
set -eu

msg_file="$1"
subject="$(head -n1 "$msg_file")"

case "$subject" in
  chore|chore:|docs|docs:|feat|feat:|fix|fix:)
    echo "ERROR: generic commit subject '$subject' is not allowed." >&2
    echo "Use: type(scope): concise message" >&2
    exit 1
    ;;
esac

exit 0